import * as Calculator from 'client/components/calculator/Calculator';

describe( 'testing calculator', () => {

    it( 'calculator test', () => {
        expect( Calculator ).toBeDefined();
    } );
} );
